@model Mvc.Study.Beginner.PageMenuItemViewModel[]

<ul class="nav nav-pills x-pagemenu">

    @foreach (var item in Model)
    {
        <li data-pageid="@item.Id"><a href="@Url.RouteUrl("Page", new { pageId = item.Id })">@item.Title</a></li>
    }

</ul>